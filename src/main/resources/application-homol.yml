spring:
  application:
    name: med-scheduler-api

  flyway:
    locations: classpath:db/migration-postgresql

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

api:
  security:
    token:
      secret: ${JWT_SECRET}

server:
  error:
    include-stacktrace: never
