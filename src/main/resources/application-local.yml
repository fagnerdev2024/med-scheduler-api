spring:
  application:
    name: med-scheduler-api

  datasource:
    url: jdbc:postgresql://localhost:5432/medscheduler_local?sslmode=disable
    username: postgres
    password: postgres

  flyway:
    locations: classpath:db/migration-postgresql,classpath:db/migration-local

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  output:
    ansi:
      enabled: always

api:
  security:
    token:
      secret: 123456

server:
  port: 8081
  error:
    include-stacktrace: never
